/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = function(knex) { //roda uma função de criação de tabela, return uma promisse
  
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = function(knex) { //rollback é uma função de desfazer a criação de tabela
  
};
